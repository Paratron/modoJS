import React from 'react';
import {shallow} from 'enzyme';

import List from './index';

describe('<List />', () =>{
	it('Renders an Array with strings correctly');
	it('Renders an Array with objects correctly');
	it('Renders a Map correctly');
	it('Renders an Object correctly');
});
